import { PriorityToFilterPipe } from './priority-to-filter.pipe';

describe('PriorityToFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityToFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
