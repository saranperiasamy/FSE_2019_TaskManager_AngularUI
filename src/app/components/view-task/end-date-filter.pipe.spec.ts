import { EndDateFilterPipe } from './end-date-filter.pipe';

describe('EndDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EndDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
