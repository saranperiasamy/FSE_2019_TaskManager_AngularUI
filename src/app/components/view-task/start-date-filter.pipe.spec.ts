import { StartDateFilterPipe } from './start-date-filter.pipe';

describe('StartDateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StartDateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
